- hosts: jboss, localhost
  become: yes 
  become_method: sudo
  tasks: 
  - name: setup jboss
    include_task:  "roles/jboss/tasks/main.yml"

- hosts: nginx, localhost
  become: yes 
  become_method: sudo
  tasks: 
  - name: setup jboss
    include_task:  "roles/nginx/tasks/main.yml"
